# tuples are like lists, but their values are immutable, they cannot be changed with other commands
# used for data that is not supposed to change
coordinates = [(4, 5), (6, 7), (80, 34)]   # use () instead of [] (used for lists)
# tuples can be stored inside lists

print(coordinates[1])




